from tf_experiments import train_setting_from_scratch as train_from_scratch, \
    train_setting_from_imagenet as train_from_imagenet, train_setting_from_cars as train_from_cars

# train_from_cars.train(5, 'cars', hours=1, minutes=10, summary_secs=20)
# train_from_imagenet.train(5, 'cars', hours=1, minutes=10, summary_secs=20)
# train_from_scratch.train(5, 'cars', hours=1, minutes=10, summary_secs=20)
# train_from_cars.train(5, 'bmw_models', hours=1, minutes=10, summary_secs=20)
# train_from_imagenet.train(5, 'bmw_models', hours=1, minutes=10, summary_secs=20)
# train_from_scratch.train(5, 'bmw_models', hours=1, minutes=10, summary_secs=20)
# train_from_cars.train(5, 'car_types', hours=1, minutes=10, summary_secs=20)
# train_from_imagenet.train(5, 'car_types', hours=1, minutes=10, summary_secs=20)
# train_from_scratch.train(5, 'car_types', hours=1, minutes=10, summary_secs=20)
# train_from_cars.train(5, 'seasons', hours=1, minutes=10, summary_secs=20)
# train_from_imagenet.train(5, 'seasons', hours=1, minutes=10, summary_secs=20)
# train_from_scratch.train(5, 'seasons', hours=1, minutes=10, summary_secs=20)
#
# train_from_cars.train(6, 'cars', hours=1, minutes=5, summary_secs=10)
# train_from_imagenet.train(6, 'cars', hours=1, minutes=5, summary_secs=10)
# train_from_scratch.train(6, 'cars', hours=1, minutes=5, summary_secs=10)
# train_from_cars.train(6, 'bmw_models', hours=1, minutes=5, summary_secs=10)
# train_from_imagenet.train(6, 'bmw_models', hours=1, minutes=5, summary_secs=10)
# train_from_scratch.train(6, 'bmw_models', hours=1, minutes=5, summary_secs=10)
# train_from_cars.train(6, 'car_types', hours=1, minutes=5, summary_secs=10)
# train_from_imagenet.train(6, 'car_types', hours=1, minutes=5, summary_secs=10)
# train_from_scratch.train(6, 'car_types', hours=1, minutes=5, summary_secs=10)
# train_from_cars.train(6, 'seasons', hours=1, minutes=5, summary_secs=10)
# train_from_imagenet.train(6, 'seasons', hours=1, minutes=5, summary_secs=10)
# train_from_scratch.train(6, 'seasons', hours=1, minutes=5, summary_secs=10)
#
#
# train_from_cars.train(7, 'cars', hours=1, minutes=1, summary_secs=5)
# train_from_imagenet.train(7, 'cars', hours=1, minutes=1, summary_secs=5)
# train_from_scratch.train(7, 'cars', hours=1, minutes=1, summary_secs=5)
# train_from_cars.train(7, 'bmw_models', hours=1, minutes=1, summary_secs=5)
# train_from_imagenet.train(7, 'bmw_models', hours=1, minutes=1, summary_secs=5)
# train_from_scratch.train(7, 'bmw_models', hours=1, minutes=1, summary_secs=5)
# train_from_cars.train(7, 'car_types', hours=1, minutes=1, summary_secs=5)
# train_from_imagenet.train(7, 'car_types', hours=1, minutes=1, summary_secs=5)
# train_from_scratch.train(7, 'car_types', hours=1, minutes=1, summary_secs=5)
# train_from_cars.train(7, 'seasons', hours=1, minutes=1, summary_secs=5)
# train_from_imagenet.train(7, 'seasons', hours=1, minutes=1, summary_secs=5)
# train_from_scratch.train(7, 'seasons', hours=1, minutes=1, summary_secs=5)

train_from_cars.train(8, 'cars', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_imagenet.train(8, 'cars', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_scratch.train(8, 'cars', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_cars.train(8, 'bmw_models', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_imagenet.train(8, 'bmw_models', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_scratch.train(8, 'bmw_models', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_cars.train(8, 'car_types', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_imagenet.train(8, 'car_types', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_scratch.train(8, 'car_types', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_cars.train(8, 'seasons', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_imagenet.train(8, 'seasons', hours=1, minutes=1, seconds=6, summary_secs=1)
train_from_scratch.train(8, 'seasons', hours=1, minutes=1, seconds=6, summary_secs=1)

